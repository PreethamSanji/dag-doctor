"""Eval harness: scorers, threshold gate, report writer.

Lands in M3. The golden set and ``evals/thresholds.yaml`` are the CI gate that
makes prompt, retrieval, and model changes regression-tested rather than vibes.
"""
